#+title: Init Session


* Load Required modules

Load org and elisp required libraries.

#+begin_src elisp
(require 'org)
(org-babel-do-load-languages 'org-babel-load-languages '((python . t)(shell . t)(C . t)))
(let ((current-dir (file-name-directory (or load-file-name (buffer-file-name)))))
(org-babel-load-file (expand-file-name "elisp/WModules.org" current-dir))
(org-babel-load-file (expand-file-name "elisp/WObject.org" current-dir))
(org-babel-lob-ingest (expand-file-name "templates/WClass.org" current-dir))
(org-babel-lob-ingest (expand-file-name "templates/WObject.org" current-dir))
(org-babel-lob-ingest (expand-file-name "templates/WModules.org" current-dir))
)
#+end_src
